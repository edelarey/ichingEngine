/**
 * 
 * @param {*} word 
 * @param {*} symbol
 * @description wordandsymbol "Descriptor of a Chinese Symbol with Word(string) Interpretation" 
 */


var wordandsymbol = function(word, symbol) {
    this.word = word;
    this.symbol = symbol;
}

export default {
    wordandsymbol,
}
